# ─────────────────────────────────────────────────────────────────────────
# FcstLabPro 复现性锁定文件 (Reproducibility Lock)
#
# 用途: 锁定能 bit-exact 复现 E1/E8 生产基线的依赖版本。
#
# 背景 (2026-05-29): 发现 E1 Kappa 从 0.3433 漂到 0.3669, 根因是
#   1) 依赖只有 >= 下限锁, 无 lockfile → 环境一升级就漂
#   2) 跑成了 Py3.9 + LightGBM 4.6 (基线是 Py3.10 + LightGBM 4.3)
# 用本文件锁定的版本 + baseline_snapshot/ 的冻结数据 = bit-exact 复现。
#
# 安装:
#   uv venv --python 3.10 .venv
#   VIRTUAL_ENV=.venv uv pip install -r requirements.lock.txt \
#     --index-url https://pypi.ci.artifacts.walmart.com/artifactory/api/pypi/external-pypi/simple \
#     --allow-insecure-host pypi.ci.artifacts.walmart.com
#
# 验证:
#   .venv/bin/python scripts/verify_reproducibility.py
#
# ⚠️ 改动任何这里的版本后, 必须重跑 verify_reproducibility.py 确认仍 bit-exact。
# ─────────────────────────────────────────────────────────────────────────
joblib==1.5.3
lightgbm==4.3.0
numpy==1.26.4
pandas==2.2.1
python-dateutil==2.9.0.post0
pytz==2026.2
pyyaml==6.0.3
scikit-learn==1.4.1.post1
scipy==1.15.3
six==1.17.0
tabulate==0.10.0
threadpoolctl==3.6.0
tzdata==2026.2
