# Data (now tracked in git)
# data/raw/*.csv  — 训练基准, tracked, sha 锁定 (勿取消注释)
# data/raw/*.parquet
# data/external/

# Live/实时下载数据 — 可变, 绝不进 git (lesson_0602: 防实时数据覆盖基准被commit回来)
data/live/

# crypto-market-data 转出的展示 CSV — 再生数据 (由 sync_market_data 从 sibling repo 重建),
# 不进 git: 免 churn + 免跟 git pull 打架 (同 data/live 理念)
data/external/cmd_*.csv

# 派生指标 — 可重算/可重建的再生数据, 不进 git (同 cmd_*.csv / data/live 理念):
#   ahr999.csv      纯公式可重算 (compute_ahr999.py)
#   reserve_risk.csv  history 基底 + REST API 可重建 (update_reserve_risk.py)
# 之前误 tracked -> VPS working tree 永久 dirty + git pull 冲突炸弹, 现降级 ignored。
# 注意: reserve_risk_history.csv 是事故抢救的冻结基底, 不可重算, 保持 tracked, 勿加这里。
data/external/onchain/ahr999.csv
data/external/onchain/reserve_risk.csv

# Experiments outputs (keep configs, ignore large files)
experiments/**/model.joblib
experiments/**/predictions.csv

# Production models — models/production/ IS tracked (promote_model.py 管理)
models/*
!models/production/
experiments/**/*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual env
.venv/
venv/
venv_py310/
lab-venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
logs/

# Signals (generated output)
signals/
data/signals/
data/live/monitoring/

# Environment / Secrets
.env
.env.*
!.env.example

# 临时备份目录 (本地应急还原用, 不进库 — git 本身即版本备份)
experiments/_backup_*/
